# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.27
itsdangerous>=2.1          # signed session cookies (team mode)
python-multipart>=0.0.9    # form uploads (screenshots)

# --- HTTP / scraping ---
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
feedparser>=6.0
playwright>=1.42           # e-commerce (lazy-imported; needs `playwright install`)

# --- Analysis / vision ---
anthropic>=0.40            # Claude API (lazy-imported)

# --- Trends ---
pytrends>=4.9              # Google Trends (lazy-imported)

# --- Media ---
Pillow>=10.0              # EXIF + image handling (lazy-imported)

# --- Export ---
openpyxl>=3.1
python-docx>=1.1          # Word (.docx) report download

# --- Config ---
pyyaml>=6.0
python-dotenv>=1.0        # optional; settings.py has a built-in fallback loader

# --- Testing ---
pytest>=8.0
httpx>=0.27               # FastAPI TestClient
